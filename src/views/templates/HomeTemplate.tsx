import type { FC } from "react";
import styles from "./Home.module.css";

const HomeTemplate: FC = () => {
  return <div className={styles.maintemplate}>صفحه اصلی</div>;
};

export default HomeTemplate;
